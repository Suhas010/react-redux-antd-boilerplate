import React from 'react';
import TargetGroupContainer from './TargetGroup/TargetGroupContainer';
import './index.scss';

const DataEntry = () => (
  <div className="container">
    <TargetGroupContainer />
  </div>
);

export default DataEntry;
